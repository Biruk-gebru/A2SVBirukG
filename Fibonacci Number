class Solution:
    def fib(self, n: int) -> int:
        def fi(n):
            if n==0:
                return 0
            if n==1:
                return 1
            return fi(n-1)+fi(n-2)
        return fi(n)
        
